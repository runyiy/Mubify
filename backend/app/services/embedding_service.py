"""TODO: Add an embedding abstraction if semantic search needs custom embeddings.

Current ChromaDB usage relies on the collection's default embedding behavior.
Do not add recommendation reranking or retrieval logic here until the service
has a clear caller.
"""
