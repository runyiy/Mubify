# Copy this file to backend/.env before running the backend locally.
# Replace the database URL and secret key with values for your environment.

DATABASE_URL=postgresql://user:password@localhost:5432/mubify
SECRET_KEY=replace-this-with-a-long-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
